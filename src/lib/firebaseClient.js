/**
 * Firebase was removed from this project (see REQUIREMENTS_ADMIN_SPLIT_IMAGES.md).
 * Realtime features that depended on RTDB are disabled until a replacement exists.
 */
export function getFirebaseApp() {
  return null;
}
